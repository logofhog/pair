<%= form_for @message, :url => messages_path(:receiver => receiver), :remote => true do |f| %>

  <div><%= f.label :subject %><br />
  <%= f.text_field :subject, autofocus: true %></div>

  <div><%= f.label :body %><br />
  <%= f.text_area :body, autofocus: true %></div>

  <div><%= f.submit "Send" %></div>
<% end %>
