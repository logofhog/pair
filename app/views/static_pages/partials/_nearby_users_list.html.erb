this is the users list
<% for user in @nearby %>
  <li>(<%= user.distance.round(2) %> miles)
  <%= user.name%>
  <%= link_to user.name, user_path(user), :remote=> true %>
  <%= link_to 'message', new_message_path(:receiver => user), :remote=> true %>
  <div id = 'new_message<%= user.id %>'></div>
  </li>
  <% end %>
